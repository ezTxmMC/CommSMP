name: ${project.name}
version: '${project.version}'
main: de.commsmp.smp.SMP
api-version: '1.21'
authors: [ ezTxmMC, SyntaxJason, TntTastisch ]
load: STARTUP

dependencies:
  server:
    LuckPerms:
      load: BEFORE
      required: true
      join-classpath: true
